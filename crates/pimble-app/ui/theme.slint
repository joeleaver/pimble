// Pimble Theme Definitions
// Color theme (VS Code Dark-inspired)

export global Theme {
    // Background colors
    out property <color> titlebar-bg: #3c3c3c;
    out property <color> menubar-bg: #3c3c3c;
    out property <color> menu-bg: #252526;
    out property <color> sidebar-bg: #252526;
    out property <color> editor-bg: #1e1e1e;
    out property <color> statusbar-bg: #007acc;
    out property <color> transparent-color: #00000000;

    // Text colors
    out property <color> text: #cccccc;
    out property <color> text-muted: #858585;
    out property <color> text-bright: #ffffff;
    out property <color> text-disabled: #6b6b6b;

    // Interactive colors
    out property <color> hover: #505050;
    out property <color> menu-hover: #094771;
    out property <color> selected: #094771;
    out property <color> focus: #007fd4;

    // Border colors
    out property <color> border: #454545;
    out property <color> menu-border: #454545;

    // Button colors
    out property <color> button-bg: #0e639c;
    out property <color> button-text: #ffffff;

    // Panel colors
    out property <color> panel-bg: #252526;

    // Scrollbar colors
    out property <color> scrollbar-thumb: #5a5a5a;
    out property <color> scrollbar-hover: #787878;
    out property <color> scrollbar-track: #1e1e1e;
}
