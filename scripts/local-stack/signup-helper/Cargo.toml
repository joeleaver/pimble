[package]
name = "signup-helper"
version = "0.0.0"
edition = "2021"
publish = false

# Its own workspace: a throwaway for the local stack, not part of the product.
[workspace]

[dependencies]
pimble-crypto = { path = "../../../crates/pimble-crypto" }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "blocking"] }
serde_json = "1"
